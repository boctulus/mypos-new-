import { GenericTypesenseAdapter } from '../../generic/GenericTypesenseAdapter.js';

/**
 * Typesense adapter specifically for Firebase datasource
 */
export class TypesenseFirebaseAdapter extends GenericTypesenseAdapter {
  // Add Firebase-specific implementations if needed
  // For now, extending the generic implementation
}