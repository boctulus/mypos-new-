import { GenericTypesenseAdapter } from '../../generic/GenericTypesenseAdapter.js';

/**
 * Typesense adapter specifically for Postgres datasource
 */
export class TypesensePostgresAdapter extends GenericTypesenseAdapter {
  // Add Postgres-specific implementations if needed
  // For now, extending the generic implementation
}