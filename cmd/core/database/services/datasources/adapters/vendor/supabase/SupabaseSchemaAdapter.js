import { GenericSchemaAdapter } from '../../generic/GenericSchemaAdapter.js';

/**
 * Schema adapter specifically for Supabase datasource
 */
export class SupabaseSchemaAdapter extends GenericSchemaAdapter {
  // Add Supabase-specific implementations if needed
  // For now, extending the generic implementation
}