import { GenericTypeValidationAdapter } from '../../generic/GenericTypeValidationAdapter.js';

/**
 * Type validation adapter specifically for Supabase datasource
 */
export class SupabaseTypeValidationAdapter extends GenericTypeValidationAdapter {
  // Add Supabase-specific implementations if needed
  // For now, extending the generic implementation
}