import { GenericTypeValidationAdapter } from '../../generic/GenericTypeValidationAdapter.js';

/**
 * Type validation adapter specifically for Firebase datasource
 */
export class FirebaseTypeValidationAdapter extends GenericTypeValidationAdapter {
  // Add Firebase-specific implementations if needed
  // For now, extending the generic implementation
}