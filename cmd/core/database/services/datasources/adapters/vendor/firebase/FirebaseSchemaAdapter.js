import { GenericSchemaAdapter } from '../../generic/GenericSchemaAdapter.js';

/**
 * Schema adapter specifically for Firebase datasource
 */
export class FirebaseSchemaAdapter extends GenericSchemaAdapter {
  // Add Firebase-specific implementations if needed
  // For now, extending the generic implementation
}